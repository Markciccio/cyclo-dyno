import type {SessionSample} from '../types';export function PowerChart({samples}:{samples:SessionSample[]}){const max=Math.max(800,...samples.map(x=>x.powerWatts)),end=Math.max(1,...samples.map(x=>x.elapsedMs)),points=samples.map(x=>`${x.elapsedMs/end*100},${100-x.powerWatts/max*96}`).join(' ');return <svg className="chart" viewBox="0 0 100 100" preserveAspectRatio="none"><path d="M0 100H100M0 50H100" className="grid"/><polyline points={points} fill="none" stroke="var(--volt)" strokeWidth="2" vectorEffect="non-scaling-stroke"/></svg>}
