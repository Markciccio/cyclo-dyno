export function Gauge({power,range,overdriveAt=range}:{power:number;range:number;overdriveAt?:number}){const extra=power>overdriveAt;return <div className={`gauge ${extra?'gauge-extra':''}`}><div className="gauge-fill" style={{width:`${Math.min(100,power/range*100)}%`}}/>{extra&&<i>+{Math.round(power-overdriveAt)} W OVERDRIVE</i>}<span>0</span><b>{range} W</b></div>}
